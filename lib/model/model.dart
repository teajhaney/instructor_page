class Model {
  String name, subject, location, image, price;

  Model(
      {required this.location,
      required this.name,
      required this.subject,
      required this.image,
      required this.price});
}
